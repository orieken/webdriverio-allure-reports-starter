version: 2
jobs:
  test:
    docker:
      - image: circleci/node:jessie-browsers
    steps:
      - checkout
      - run:
          name: Install Node Modules
          command: npm install
      - run:
          name: Test
          command: npm test
      - store_test_results:
          path: allure-report
workflows:
  version: 2
  test:
    jobs:
      - test
